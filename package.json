{
  "name": "code_name",
  "homepage": "https://rtwwa.github.io/code_name/",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
		"predeploy": "npm run build",
		"deploy": "gh-pages -d dist",
    "zip": "npm run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\""
  },
  "dependencies": {
    "gh-pages": "^6.3.0",
    "kaplay": "^3001.0.19"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "vite": "^7.1.1"
  }
}
